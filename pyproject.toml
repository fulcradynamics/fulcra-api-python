[tool.poetry]
name = "fulcra-api"
version = "0.1.2"
description = ""
authors = ["brandon <brandon@fulcradynamics.com>"]
readme = "README.md"
packages = [{include = "fulcra_api"}]

[tool.poetry.dependencies]
python = "^3.9"
pandas = "> 1.5, < 3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
